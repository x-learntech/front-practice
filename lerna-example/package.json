{
  "private": true,
  "name": "vv-desktop-web-core",
  "version": "1.0.0",
  "scripts": {
    "publish": "lerna publish",
    "build:publish": "pnpm run build:all & lerna publish",
    "clean": "lerna clean",
    "build:atoms": "lerna run build --scope @vv-desktop-web-core/atoms",
    "build:hooks": "lerna run build --scope @vv-desktop-web-core/hooks",
    "build:all": "pnpm run build:hooks && pnpm run build:atoms"
  },
  "files": [
    "dist",
    "es",
    "lib",
    "compiled"
  ],
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "lerna": "^7.1.5",
    "father": "^4.3.6"
  }
}
